\* BROKEN variant, stage 3 of 3 -- the losing trace itself.
\* Same injected defect (ACK on receipt).  Both OrderingInv2 and RetentionSafety
\* are removed so that TLC runs past them and exhibits the ACTUAL data loss:
\* after Freeze, a message whose send() returned exists nowhere in the system --
\* not in sender retention, not on either wire, not in the parser, and not in
\* the receiver's CRIU image.  NoLossSafety must be violated.
SPECIFICATION Spec

CONSTANTS
    MaxMsg       = 3
    W            = 2
    Reserve      = 2
    MaxFreezes   = 1
    AckOnReceipt = TRUE
    DrainQueueVolatile = FALSE
    PruneOnTransmit    = FALSE

INVARIANTS
    TypeOK
    NoLossSafety
    NoDuplication
    FIFO
    NoUnboundedBacklog
    CreditSound

PROPERTIES
    DeliveryObligation
