\* AUDIT-PASS FAULT INJECTION 2, stage 2 of 2.
\* Same injected defect as SequencedLink_broken_prune_on_transmit.cfg (retention
\* released at socket-write time).  OrderingInv1 is REMOVED, because it is a
\* near-restatement of the injected bug and trips at depth 3 before anything
\* observable happens.  What this run shows is that the defect propagates all
\* the way to ACTUAL LOSS: after a Freeze, a message whose send() returned is
\* nowhere in the system.  NoLossSafety must be violated.
SPECIFICATION Spec

CONSTANTS
    MaxMsg       = 3
    W            = 2
    Reserve      = 2
    MaxFreezes   = 1
    AckOnReceipt = FALSE
    DrainQueueVolatile = FALSE
    PruneOnTransmit    = TRUE

INVARIANTS
    TypeOK
    NoLossSafety
    NoDuplication
    FIFO
    NoUnboundedBacklog
