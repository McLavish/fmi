apiVersion: apps/v1
kind: Deployment
metadata:
  name: fmi-redis
  namespace: fmi-criu
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fmi-redis
  template:
    metadata:
      labels:
        app: fmi-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          args: ["--save", "", "--appendonly", "no"]
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: fmi-redis
  namespace: fmi-criu
spec:
  selector:
    app: fmi-redis
  ports:
    - port: 6379
      targetPort: 6379
